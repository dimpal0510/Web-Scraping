# Create the file (using a text editor or touch command)
touch new_file.txt

# Stage the file
git add new_file.txt

# Commit the changes
git commit -m "Add new file new_file.txt"

# Push the changes to the remote repository
git push origin main
